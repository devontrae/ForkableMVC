<?php

	/* 
	 *
	 *	This is where we will define constants
	 *
	 */

	
	# ------------------
	# Database Constants
	# ------------------

	define(DB_HOST, $CONFIG_database['host']);
	define(DB_USER, $CONFIG_database['user']);
	define(DB_PASSWORD, $CONFIG_database['password']);
	define(DB_DATABASE, $CONFIG_database['database']);
	define(DB_ENABLED, $CONFIG_database['enabled']);